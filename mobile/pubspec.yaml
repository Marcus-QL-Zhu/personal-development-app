name: personal_development_app
description: Personal Development App mobile shell
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  audioplayers: ^6.1.0
  audio_session: ^0.1.25
  file_picker: ^10.3.2
  flutter:
    sdk: flutter
  just_audio: ^0.9.40
  path_provider: ^2.1.4
  record: ^6.2.0
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
